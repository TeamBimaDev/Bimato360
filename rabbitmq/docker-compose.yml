version: '3'

services:
  rabbitmq:
    build:
      context: .
    ports:
      - "15672:15672"
      - "5672:5672"
    networks:
      - bima360_network-external
networks:
  bima360_network-external:
    external:
      name: bima360-network
